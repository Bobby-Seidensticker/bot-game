namespace.module('bot.controls', function (exports, require) {

    var funcs = require('org.startpad.funcs').patch();

    exports.extend({
        'init': init,
        'tick': tick
    });

    function init() {}

    function tick() {}

});